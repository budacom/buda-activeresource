name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: read
  id-token: write
  actions: read

jobs:
  claude:
    uses: budacom/github-actions/.github/workflows/reusable_claude_review.yaml@workflows-v8
    with:
      model: claude-haiku-4-5
    secrets: inherit
